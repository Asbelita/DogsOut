# Uncomment the next line to define a global platform for your project
# platform :ios, '10.0'

target 'DogsOut' do
  
  use_frameworks!
  
  pod 'Alamofire', '~> 5.2'
  
  target 'DogsOutTests' do
    inherit! :search_paths
  end
  
  target 'DogsOutUITests' do
  end
  
end
